---
name: Dectris Release

on:
  push:
    tags:
    - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 0.43.8-dc.1). A 'v' prefix will be added for the tag/release."
        required: true
        type: string
        default: '0.43.8-dc.1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

jobs:
  build-libraries:
    runs-on: ubuntu-latest
    # Scoped to this job rather than the workflow: only the upload step needs
    # write, and a workflow-level grant would extend it to any job added later.
    permissions:
      contents: write # required by the upload step to create the tag and release
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      with:
        submodules: true
        persist-credentials: false

    - name: Set up Go
      uses: actions/setup-go@7b8cf10d4e4a01d4992d18a89f4d7dc5a3e6d6f4  # v4
      with:
        go-version: '1.25'
        # This workflow publishes release artifacts; a poisoned restore could
        # taint the binary that gets uploaded.
        cache: false

    - name: Get dependencies
      run: go mod download

    - name: Build Linux AMD64 static binary
      run: |
        env CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -ldflags "-s -w -X main.Version=`git rev-parse HEAD`" -o geesefs .

    - name: Generate checksums
      run: |
        sha256sum geesefs > checksums.txt

    - name: Determine version
      id: version
      env:
        EVENT_NAME: ${{ github.event_name }}
        INPUT_VERSION: ${{ inputs.version }}
      run: |
        if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
          echo "tag=v${INPUT_VERSION}" >> "$GITHUB_OUTPUT"
          echo "prerelease=true" >> "$GITHUB_OUTPUT"
        else
          echo "tag=${GITHUB_REF/refs\/tags\//}" >> "$GITHUB_OUTPUT"
          echo "prerelease=false" >> "$GITHUB_OUTPUT"
        fi

    - name: Upload library artifacts to release
      uses: softprops/action-gh-release@de2c0eb89ae2a093876385947365aca7b0e5f844  # v1
      # Publish on a tag push, and on manual dispatch, where the tag is taken
      # from the version input and created at the dispatched ref.
      if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
      with:
        tag_name: ${{ steps.version.outputs.tag }}
        prerelease: ${{ steps.version.outputs.prerelease == 'true' }}
        files: |
          geesefs
          checksums.txt
        body: |
          ## Static Binary

          This release includes a statically linked binary:

          ### Static Binary - Linux AMD64
          - `geesefs` - Standalone executable with all dependencies statically linked

          ### Verification
          - `checksums.txt` - SHA256 checksum for the binary

          ### Usage
          **Direct execution:** Download `geesefs`, make it executable (`chmod +x geesefs`), and run it directly.

          **For Go projects:** Use `go get github.com/yandex-cloud/geesefs`

          The binary is completely self-contained with no external dependencies required.
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
